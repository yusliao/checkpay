---
description: 代码与配置变更时同步更新仓库文档（无需用户提醒）
alwaysApply: true
---

# 文档同步（CheckPay）

凡涉及功能行为、配置项、部署方式、环境变量、对外 API 或用户可见流程的改动，应在**同一批提交**内同步更新相关文档与示例，包括但不限于：

- 根目录 `README.md`、`CLAUDE.md`、`AGENTS.md`
- `docs/` 中与该改动对应的设计或流程说明（若存在且仍适用）
- `docker-compose.yml`、`.env.example` 及 `appsettings*.json` 中的说明、占位符与注释

若仅为内部重构且对外契约与运维方式不变，可只更新 `CLAUDE.md` 顶部变更记录一行，或注明「无文档变更」类说明；禁止长期让文档与代码明显脱节。
